# 第一版验证记录

验证日期：2026-09-11。本机原生运行：Node.js 24、Python 3.14、PostgreSQL 18.1。

- 前端 TypeScript 与 Vite 生产构建通过。
- 自动化测试：55 项通过，覆盖查询计划、SQL AST 限制、月份和季度比较、参数绑定、零分母、金额独立查询核对、目标粒度、数据一致性、数据库只读权限、账号会话隔离、收藏去重、导出、反馈、网络重试与取消。
- 真实模型：30 个标准问题和4轮连续追问，首轮31/34；3项连接失败后复测通过，最终34/34。原始首轮错误和复测标记保留在 model-evaluation.json。
- 浏览器验证：用户登录、真实华东收入同比趋势、表格切换、SQL展开、历史会话恢复、上海追问继承同比条件、停止生成及停止记录持久化。
- 窄屏检查：390px宽视口，文档无横向溢出；桌面使用默认视口检查。
- 环境生成脚本验证：数据库密码与连接串一致、已有配置不覆盖；启动shell语法检查通过。

本地网站 http://127.0.0.1:5178/，API http://127.0.0.1:8000/ 。所有业务数据均为模拟生成。

当时尚未验证 Docker 镜像构建与 Compose 实际启动；该项已在下方增量验证中完成。云端部署、多人压力测试和故障恢复演练仍未执行。Vite 提示主包体积超过500KB，可在后续版本进一步分包；图表已按需加载。测试依赖存在2条上游弃用提示，不影响当前通过结果。

## OrbStack 与 pgvector 增量验证

验证日期：2026-09-12。本机 OrbStack 2.2.3、Docker Engine 29.4.0、PostgreSQL 18、pgvector 0.8.6。

- 原 embedded-postgres 的20张表已导出并导入 Compose 命名卷；3个会话、12条消息、6条问数执行记录及全部业务事实逐表计数一致。备份保存在 `.runtime/pre-orbstack-backup.jsonl.gz` 并通过 gzip 与 SHA-256 校验。
- Alembic 0005 删除签约前商机域，0008 增加用户工作台设置和常见问题统计，数据库共27张领域表；业务语义文档使用百炼 qwen3.7-text-embedding-flash 生成1024维向量。
- 容器内57项测试通过，覆盖原有业务和权限检查以及新增表目录注释；前端在 Docker 多阶段构建中通过 TypeScript 和 Vite 生产构建。
- 真实问题“华东区今年各产品线确认收入”完成 pgvector→Qwen→结构化计划→SQLGlot→只读SQL 全链路，返回5个产品线；召回审计精确命中“确认收入、华东、产品线”。

尚未验证：云端部署、多人压力测试、备份恢复演练和大规模语义目录下的 HNSW 性能。当前千级目录使用精确余弦检索，以保持确定的召回率。
