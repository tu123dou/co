# 项目开发指令

本文件适用于整个仓库。以下内容是实施约束，不是可选建议。

## 修改前必须阅读

- 修改 `frontend/` 下的内容前，必须完整阅读 `docs/frontend-conventions.md`。
- 修改 `backend/`、`backend/alembic/` 或数据库相关脚本前，必须完整阅读 `docs/backend-conventions.md`。
- 同时修改前后端时，必须先阅读上述两份文档。
- 产品范围、数据语义和安全边界以 `README.md` 与 `docs/architecture.md` 为准。

## 改动边界

- 编辑前先检查当前实现和工作区状态，保留与本次任务无关的用户改动。
- 除非需求明确要求，否则不要修改后端接口契约、数据库结构、指标定义或认证行为。
- 不得把不支持的行为静默替换成其他查询或近似结果。
- 只有在现有技术栈无法合理完成明确需求时才能增加生产依赖，并在交付说明中解释原因。
- 不得提交密钥或输出 `.env` 中的值；配置文档使用 `.env.example`。
- 优先完成最小且完整的改动，不在实现功能时顺带重构无关模块。

## 代码质量

- 保持 TypeScript 严格模式。不得使用 `any`、不安全类型断言或非空断言绕过不明确的接口契约。
- API 传输、状态转换、浏览器副作用和视觉渲染必须保持清晰边界。
- 日期、数量、模型名称、数据覆盖范围和业务标签应来自接口数据或共享定义，不得在 UI 中硬编码。
- 注释用于解释约束或不直观的设计原因，不复述代码本身。
- 新实现验证通过后，应删除已替代的旧实现和引用，不保留同一功能的两套活动代码。

## 验证要求

- 仅修改前端：运行 `cd frontend && npm run format:check && npm run build`。
- 修改后端单元逻辑：运行 `PYTHONPATH=backend .venv/bin/pytest backend/tests -q`。
- 修改数据库集成逻辑：还要针对已确认的非生产数据库运行 `TEST_DATABASE=1 PYTHONPATH=backend .venv/bin/pytest backend/tests -q`。
- 修改 Ask 页面时，必须使用真实本地后端检查受影响流程；涉及流式、错误、取消或历史会话时应覆盖对应路径。
- 除非任务明确要求，否则不要运行真实模型评估，因为它会消耗外部 API 额度。
- 最终交付说明必须列出已验证内容，以及仍存在的警告或未验证路径。

## 安全要求

- 保持会话、收藏、反馈、设置和导出功能的用户所有权校验。
- 保持参数化 SQL、只读查询连接、SQL 校验、超时和结果数量限制。
- 不得向浏览器暴露模型凭据、数据库凭据、原始上游错误或内部密钥。
- 不得为了让测试通过而删除或覆盖持久化数据。需要联调时使用隔离的测试记录，并只清理该条明确记录。
